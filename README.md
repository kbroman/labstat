## R/labstat: an R package of data and a tiny bit of code for an old
   Statistics for Lab Scientists course

To install the package:

```{R}
install.packages("devtools")
devtools:install_github("kbroman/labstat")
```
